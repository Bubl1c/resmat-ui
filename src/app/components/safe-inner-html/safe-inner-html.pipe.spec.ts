import { SafeInnerHtmlPipe } from './safe-inner-html.pipe';

describe('SafeInnerHtmlPipe', () => {
  it('create an instance', () => {
    // const pipe = new SafeInnerHtmlPipe();
    // expect(pipe).toBeTruthy();
  });
});
